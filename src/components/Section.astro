---
import type { HTMLAttributes } from 'astro/types'

interface Props extends HTMLAttributes<'section'> {
  title: string
  meta?: string
}

const { title, meta, ...attrs } = Astro.props
---

<section {...attrs}>
  <header>
    <h2>{title}</h2>
    {meta && <span>{meta}</span>}
  </header>
  <slot />
</section>

<style>
  section > header {
    display: flex;
    align-items: end;
    justify-content: space-between;
    gap: 16px;

    margin-bottom: 16px;

    h2 {
      color: var(--forest);
      font-size: 2rem;
      font-weight: normal;
    }

    span {
      color: var(--text-muted);
      font-size: 0.72rem;
      font-family: var(--mono);
    }
  }
</style>
